---
const siteTitle = "Sukhminder Singh | Webapps";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <title>{siteTitle}</title>
  </head>
  <body>
    <main class="page">
      <header class="hero">
        <p class="kicker">Personal Lab</p>
        <h1>Sukhminder Singh</h1>
        <p>
          Interactive engineering webapps that run in the browser. Focus areas: mechanics,
          numerical methods, and optimization.
        </p>
      </header>

      <section class="apps" aria-label="Webapps">
        <article class="app-card">
          <h2>2D Topology Optimization</h2>
          <p>
            Density-based SIMP optimization for linear elasticity. Includes volume constraint,
            filtering, and live convergence plot.
          </p>
          <a href="/apps/topopt">Open App</a>
        </article>
      </section>
    </main>

    <style>
      :root {
        --bg: #f4f6f8;
        --surface: #ffffff;
        --ink: #0f172a;
        --muted: #4b5563;
        --accent: #0f766e;
        --accent-strong: #115e59;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: "IBM Plex Sans", "Segoe UI", sans-serif;
        background:
          radial-gradient(circle at 8% 8%, #d1fae5 0, rgba(209, 250, 229, 0) 42%),
          radial-gradient(circle at 86% 14%, #ccfbf1 0, rgba(204, 251, 241, 0) 40%),
          var(--bg);
        color: var(--ink);
      }

      .page {
        max-width: 900px;
        margin: 0 auto;
        padding: 48px 20px 72px;
      }

      .hero {
        padding: 30px;
        background: var(--surface);
        border-radius: 18px;
        border: 1px solid #dbe3e8;
        box-shadow: 0 12px 32px rgba(15, 23, 42, 0.08);
      }

      .kicker {
        margin: 0;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--accent-strong);
        font-size: 0.78rem;
        font-weight: 700;
      }

      h1 {
        margin: 10px 0 12px;
        font-size: clamp(2rem, 5vw, 3.1rem);
      }

      .hero p {
        margin: 0;
        color: var(--muted);
        max-width: 70ch;
        line-height: 1.5;
      }

      .apps {
        margin-top: 22px;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 18px;
      }

      .app-card {
        background: var(--surface);
        border: 1px solid #dbe3e8;
        border-radius: 14px;
        padding: 20px;
      }

      .app-card h2 {
        margin: 0 0 8px;
        font-size: 1.3rem;
      }

      .app-card p {
        margin: 0 0 14px;
        color: var(--muted);
        line-height: 1.45;
      }

      .app-card a {
        display: inline-block;
        text-decoration: none;
        color: #fff;
        background: var(--accent);
        border-radius: 8px;
        font-weight: 600;
        padding: 10px 14px;
      }

      .app-card a:hover {
        background: var(--accent-strong);
      }
    </style>
  </body>
</html>
